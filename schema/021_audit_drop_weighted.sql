-- Взвешенного вклада больше нет: множитель weight= у вызова инспектора снят,
-- и число, которое ложится в сумму фазы, равно тому, что инспектор прислал
-- (у совещательного с deny -- сотня). Оно живёт в inspectors_score; вторая
-- карта с тем же числом только заставляла бы спрашивать, чем они различаются.
ALTER TABLE waf.audit DROP COLUMN IF EXISTS inspectors_weighted;
