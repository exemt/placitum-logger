-- Поля запроса, выбранные оператором через waf_var. Модуль их не толкует и
-- набор у каждого контура свой, поэтому карта, а не колонки: схема логгера не
-- должна меняться от того, что на краю добавили $ssl_ja3.
--
-- Ключ LowCardinality: имён немного (waf_var ограничен шестнадцатью), значения
-- произвольные.

ALTER TABLE waf.audit
    ADD COLUMN IF NOT EXISTS vars Map(LowCardinality(String), String);
